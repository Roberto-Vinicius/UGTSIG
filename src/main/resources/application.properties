# Nome da aplicação Spring
spring.application.name=formulario

# ==============================Configuração do banco de dados PostgreSQL=====================================
# URL de conexão com o banco de dados PostgreSQL
#spring.datasource.url=jdbc:postgresql://localhost:4002/postgres
# Usuário do banco de dados
#spring.datasource.username=postgres
# Senha do banco de dados
#spring.datasource.password=dev123
# Configuração do Hibernate para lidar com LOBs (Large Objects)
#spring.jpa.properties.hibernate.jdbc.lob.non_contextual=true
# Driver JDBC para PostgreSQL
#spring.datasource.driver-class-name=org.postgresql.Driver
# Estratégia para atualização automática do schema do banco (update = atualiza schema existente)
#spring.jpa.hibernate.ddl-auto=update
# Mostra as SQLs geradas pelo Hibernate no console (útil para desenvolvimento)
#spring.jpa.show-sql=true


# ==========  Configuração do banco de dados H2 em memória =============
spring.datasource.url=jdbc:h2:mem:formulario;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.username=sa
spring.datasource.password=
spring.datasource.driver-class-name=org.h2.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
# ======================================================================

# Configuração do console do H2 (interface web)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# ========= Configurações para upload de arquivos ==========
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# ========= Configurações para envio de e-mail (SMTP do Gmail) =========
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=testrabalho10@gmail.com
spring.mail.password=yaxj gixo amhb duuw
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Configuração de logging para mostrar mensagens de validação em DEBUG
logging.level.org.springframework.validation: DEBUG

# ========= Configurações do Thymeleaf (template engine) ===========
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.cache=false

# Desabilita a página de erro padrão do Spring Boot
server.error.whitelabel.enabled=false